"use strict";
const express = require("express");
const util_1 = require("../utils/util");
let router = express.Router();
router.post('/', (request, response) => {
    util_1.default.isNullOrUndefined({});
    try {
    }
    catch (e) {
    }
});
module.exports = router;
